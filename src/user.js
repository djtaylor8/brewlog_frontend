class User {
    constructor(user) {
        this.userId = user.id;
        this.name = user.name;
        this.entries = user.entries;
    }

    // loadEntries(entries, userId) {
    //     new Entries(entries, userId)
    // }
}